# open-pr flow — live validation

This file exists only to give the `open-pr` driver flow a real, one-line diff to
open a pull request for, as an end-to-end validation of the flow inversion's
step 4. The branch and its PR are throwaway and are deleted after the run.
